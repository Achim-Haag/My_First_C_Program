# Visualstudio Code internal work areas except some json
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
!.vscode/*.code-snippets

# CMake created files (.exe will be copied to source folder by CMake add_custom_command
build/*
out/*

# Local History for Visual Studio Code
.history/

# Built Visual Studio Code Extensions
*.vsix
# AH own excludes (no internal MVSC files)
# .pdb = debugging/profiling, generated by C-Compiler
# .ilk = incremental link info database, generated by C-Compiler
# .obj = object files of external modules, linked into .exe, so we don't need it
# .swp = vim Swapfile while editing or after vim-crash
*.pdb
*.ilk
*.obj
*.swp
#
